{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "gpx",
            "database": "${workspaceFolder:gpx-to-map-viewer}/server/database.db"
        }
    ]
}